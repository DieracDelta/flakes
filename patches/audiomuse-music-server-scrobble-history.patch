diff --git a/music-server-backend/main.go b/music-server-backend/main.go
index bb9b339..743c5b3 100644
--- a/music-server-backend/main.go
+++ b/music-server-backend/main.go
@@ -166,6 +166,7 @@ func main() {
 		subsonicCompatibilityHandler(subsonic, "GET", "/hlsPlaylist", subsonicHLSPlaylist) // NEW: HLS playlist
 		subsonicCompatibilityHandler(subsonic, "GET", "/hlsSegment", subsonicHLSSegment)   // NEW: HLS segments
 		subsonicCompatibilityHandler(subsonic, "GET", "/scrobble", subsonicScrobble)
+		subsonicCompatibilityHandler(subsonic, "GET", "/getNowPlaying", subsonicGetNowPlaying)
 
 		// Browsing endpoints
 		subsonicCompatibilityHandler(subsonic, "GET", "/getMusicFolders", subsonicGetMusicFolders)
diff --git a/music-server-backend/models.go b/music-server-backend/models.go
index 99ec6ee..e262aff 100644
--- a/music-server-backend/models.go
+++ b/music-server-backend/models.go
@@ -305,6 +305,26 @@ type SubsonicScanStatus struct {
 	Count    int64    `xml:"count,attr" json:"count"`
 }
 
+type SubsonicNowPlaying struct {
+	XMLName xml.Name                  `xml:"nowPlaying" json:"-"`
+	Entries []SubsonicNowPlayingEntry `xml:"entry" json:"entry"`
+}
+
+type SubsonicNowPlayingEntry struct {
+	XMLName    xml.Name `xml:"entry" json:"-"`
+	ID         string   `xml:"id,attr" json:"id"`
+	CoverArt   string   `xml:"coverArt,attr,omitempty" json:"coverArt,omitempty"`
+	Title      string   `xml:"title,attr" json:"title"`
+	Artist     string   `xml:"artist,attr" json:"artist"`
+	Album      string   `xml:"album,attr" json:"album"`
+	Duration   int      `xml:"duration,attr,omitempty" json:"duration,omitempty"`
+	PlayCount  int      `xml:"playCount,attr,omitempty" json:"playCount,omitempty"`
+	LastPlayed string   `xml:"lastPlayed,attr,omitempty" json:"lastPlayed,omitempty"`
+	MinutesAgo int      `xml:"minutesAgo,attr" json:"minutesAgo"`
+	PlayerID   string   `xml:"playerId,attr" json:"playerId"`
+	Username   string   `xml:"username,attr" json:"username"`
+}
+
 type SubsonicUsers struct {
 	XMLName xml.Name       `xml:"users" json:"-"`
 	Users   []SubsonicUser `xml:"user" json:"user"`
diff --git a/music-server-backend/subsonic_extra_handlers.go b/music-server-backend/subsonic_extra_handlers.go
index 8e300ce..1aeaa60 100644
--- a/music-server-backend/subsonic_extra_handlers.go
+++ b/music-server-backend/subsonic_extra_handlers.go
@@ -62,31 +62,6 @@ func subsonicGetArtistInfo2(c *gin.Context) {
 	subsonicRespond(c, newSubsonicResponse(&SubsonicArtistInfo2{SubsonicArtistInfoBase: base}))
 }
 
-// --- getNowPlaying ----------------------------------------------------------
-
-// SubsonicNowPlaying is the getNowPlaying response. This server does not track
-// live streaming sessions, so the entry list is empty (spec-valid).
-type SubsonicNowPlaying struct {
-	XMLName xml.Name                  `xml:"nowPlaying" json:"-"`
-	Entries []SubsonicNowPlayingEntry `xml:"entry" json:"entry"`
-}
-
-// SubsonicNowPlayingEntry is a Child plus the now-playing attributes.
-type SubsonicNowPlayingEntry struct {
-	XMLName    xml.Name `xml:"entry" json:"-"`
-	ID         string   `xml:"id,attr" json:"id"`
-	IsDir      bool     `xml:"isDir,attr" json:"isDir"`
-	Title      string   `xml:"title,attr" json:"title"`
-	Username   string   `xml:"username,attr" json:"username"`
-	MinutesAgo int      `xml:"minutesAgo,attr" json:"minutesAgo"`
-	PlayerID   int      `xml:"playerId,attr" json:"playerId"`
-}
-
-func subsonicGetNowPlaying(c *gin.Context) {
-	_ = c.MustGet("user")
-	subsonicRespond(c, newSubsonicResponse(&SubsonicNowPlaying{Entries: []SubsonicNowPlayingEntry{}}))
-}
-
 // --- getBookmarks -----------------------------------------------------------
 
 // SubsonicBookmarks is the getBookmarks response. Bookmarks are not stored, so
diff --git a/music-server-backend/subsonic_music_handlers.go b/music-server-backend/subsonic_music_handlers.go
index 99cbef7..93367a1 100644
--- a/music-server-backend/subsonic_music_handlers.go
+++ b/music-server-backend/subsonic_music_handlers.go
@@ -16,6 +16,7 @@ import (
 	"sort"
 	"strconv"
 	"strings"
+	"sync"
 	"time"
 	"unicode"
 
@@ -34,6 +35,19 @@ type AudioInfo struct {
 	Duration int // Duration in seconds
 }
 
+type nowPlayingState struct {
+	SongID    string
+	UserID    int
+	Username  string
+	StartedAt time.Time
+	PlayerID  string
+}
+
+var (
+	nowPlayingMu     sync.Mutex
+	nowPlayingByUser = map[int]nowPlayingState{}
+)
+
 // getDuration extracts the duration of an audio file using ffprobe
 func getDuration(filePath string) int {
 	cmd := exec.Command("ffprobe",
@@ -931,23 +945,116 @@ func subsonicScrobble(c *gin.Context) {
 		return
 	}
 
-	now := time.Now().Format(time.RFC3339)
+	playedAt := time.Now()
+	if timeParam := c.Query("time"); timeParam != "" {
+		if unixMillis, err := strconv.ParseInt(timeParam, 10, 64); err == nil && unixMillis > 0 {
+			playedAt = time.UnixMilli(unixMillis)
+		} else {
+			log.Printf("Ignoring invalid Subsonic scrobble time parameter %q for song '%s'", timeParam, songID)
+		}
+	}
+	now := playedAt.Format(time.RFC3339)
+	submission := strings.ToLower(c.DefaultQuery("submission", "true")) != "false"
 
-	err := UpdateSongPlayCount(db, songID, now)
-	if err != nil {
-		log.Printf("Error updating play count for user '%s' on song '%s': %v", user.Username, songID, err)
+	if submission {
+		err := UpdateSongPlayCount(db, songID, now)
+		if err != nil {
+			log.Printf("Error updating play count for user '%s' on song '%s': %v", user.Username, songID, err)
+		}
+
+		// Track play history for this user
+		err = InsertPlayHistory(db, user.ID, songID, now)
+		if err != nil {
+			log.Printf("Error inserting play history for user '%s' on song '%s': %v", user.Username, songID, err)
+		}
 	}
 
-	// Track play history for this user
-	err = InsertPlayHistory(db, user.ID, songID, now)
-	if err != nil {
-		log.Printf("Error inserting play history for user '%s' on song '%s': %v", user.Username, songID, err)
+	// Keep a lightweight now-playing view for Subsonic clients such as multi-scrobbler.
+	// Do not let historical imports with old timestamps appear as currently playing.
+	if time.Since(playedAt) >= 0 && time.Since(playedAt) < 24*time.Hour {
+		playerID := c.Query("playerId")
+		if playerID == "" {
+			playerID = user.Username
+		}
+		nowPlayingMu.Lock()
+		nowPlayingByUser[user.ID] = nowPlayingState{
+			SongID:    songID,
+			UserID:    user.ID,
+			Username:  user.Username,
+			StartedAt: playedAt,
+			PlayerID:  playerID,
+		}
+		nowPlayingMu.Unlock()
 	}
 
-	log.Printf("Scrobbled song '%s' for user '%s'", songID, user.Username)
+	if submission {
+		log.Printf("Scrobbled song '%s' for user '%s' at %s", songID, user.Username, now)
+	} else {
+		log.Printf("Updated now playing song '%s' for user '%s'", songID, user.Username)
+	}
 	subsonicRespond(c, newSubsonicResponse(nil))
 }
 
+func subsonicGetNowPlaying(c *gin.Context) {
+	now := time.Now()
+
+	nowPlayingMu.Lock()
+	states := make([]nowPlayingState, 0, len(nowPlayingByUser))
+	for userID, state := range nowPlayingByUser {
+		if now.Sub(state.StartedAt) > 24*time.Hour {
+			delete(nowPlayingByUser, userID)
+			continue
+		}
+		states = append(states, state)
+	}
+	nowPlayingMu.Unlock()
+
+	entries := make([]SubsonicNowPlayingEntry, 0, len(states))
+	for _, state := range states {
+		var title, artist, album string
+		var duration, playCount int
+		var lastPlayed sql.NullString
+
+		err := db.QueryRow(`
+			SELECT title, artist, album, COALESCE(duration, 0), play_count, last_played
+			FROM songs
+			WHERE id = ? AND cancelled = 0
+		`, state.SongID).Scan(&title, &artist, &album, &duration, &playCount, &lastPlayed)
+		if err != nil {
+			continue
+		}
+
+		activeFor := time.Duration(duration+300) * time.Second
+		if duration <= 0 {
+			activeFor = 30 * time.Minute
+		}
+		if now.Sub(state.StartedAt) > activeFor {
+			nowPlayingMu.Lock()
+			if current, ok := nowPlayingByUser[state.UserID]; ok && current.SongID == state.SongID && current.StartedAt.Equal(state.StartedAt) {
+				delete(nowPlayingByUser, state.UserID)
+			}
+			nowPlayingMu.Unlock()
+			continue
+		}
+
+		entries = append(entries, SubsonicNowPlayingEntry{
+			ID:         state.SongID,
+			CoverArt:   state.SongID,
+			Title:      title,
+			Artist:     artist,
+			Album:      album,
+			Duration:   duration,
+			PlayCount:  playCount,
+			LastPlayed: lastPlayed.String,
+			MinutesAgo: int(now.Sub(state.StartedAt).Minutes()),
+			PlayerID:   state.PlayerID,
+			Username:   state.Username,
+		})
+	}
+
+	subsonicRespond(c, newSubsonicResponse(&SubsonicNowPlaying{Entries: entries}))
+}
+
 func subsonicGetArtists(c *gin.Context) {
 	_ = c.MustGet("user") // Auth is handled by middleware
 
