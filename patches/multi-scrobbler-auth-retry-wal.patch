diff --git a/src/backend/common/AbstractInitializable.ts b/src/backend/common/AbstractInitializable.ts
index a034130..c9a6414 100644
--- a/src/backend/common/AbstractInitializable.ts
+++ b/src/backend/common/AbstractInitializable.ts
@@ -292,13 +292,12 @@ export default abstract class AbstractInitializable {
         }
 
         if(this.authFailure) {
-            if(!force) {
-                if(this.requiresAuthInteraction) {
-                    throw new AuthCheckError('Authentication failure: Will not retry auth because user interaction is required for authentication');
-                }
-                throw new AuthCheckError('Authentication failure: Will not retry auth because authentication previously failed and must be reauthenticated');
+            if(!force && this.requiresAuthInteraction) {
+                throw new AuthCheckError('Authentication failure: Will not retry auth because user interaction is required for authentication');
             }
-            this.logger.debug('Auth previously failed for non upstream/network reasons but retry is being forced');
+            this.logger.debug(force
+                ? 'Auth previously failed for non upstream/network reasons but retry is being forced'
+                : 'Retrying unattended authentication after a previous failure');
         }
 
         try {
diff --git a/src/backend/common/database/drizzle/drizzleUtils.ts b/src/backend/common/database/drizzle/drizzleUtils.ts
index 516e55a..a2bfaf9 100644
--- a/src/backend/common/database/drizzle/drizzleUtils.ts
+++ b/src/backend/common/database/drizzle/drizzleUtils.ts
@@ -78,7 +78,15 @@ export const getDb = (dbVal: string, opts: { logger?: Logger, backupPath?: strin
   const {
     logger = loggerNoop,
   } = opts;
-  return drizzle(dbVal, {relations: relations, logger: createDrizzleLogger(logger)});
+  const db = drizzle(dbVal, {relations: relations, logger: createDrizzleLogger(logger)});
+  if(dbVal !== MEMORY_DB_NAME) {
+    db.$client.exec(`
+      PRAGMA journal_mode = WAL;
+      PRAGMA synchronous = FULL;
+      PRAGMA busy_timeout = 5000;
+    `);
+  }
+  return db;
 }
 
 export type DbConcrete = ReturnType<typeof getDb>;
diff --git a/src/backend/tests/database/drizzle.test.ts b/src/backend/tests/database/drizzle.test.ts
index 2df818d..2359272 100644
--- a/src/backend/tests/database/drizzle.test.ts
+++ b/src/backend/tests/database/drizzle.test.ts
@@ -26,6 +26,22 @@ import { migrateApp } from '../../common/database/appMigrator.js';
 // would be great to push migrations directly from schema but doesn't seem supported in newest beta
 // https://github.com/drizzle-team/drizzle-orm/discussions/4373
 
+describe('Database configuration', function () {
+
+    it('uses WAL with full durability for file databases', async function () {
+        await withLocalTmpDir(async () => {
+            const db = getDb(getDbPath('wal-test', process.cwd()));
+            const pragma = (name: string) => Object.values(db.$client.prepare(`PRAGMA ${name}`).get())[0];
+
+            expect(pragma('journal_mode')).eq('wal');
+            expect(pragma('synchronous')).eq(2);
+            expect(pragma('busy_timeout')).eq(5000);
+
+            db.$client.close();
+        }, { unsafeCleanup: true, postfix: 'walConfig' });
+    });
+});
+
 describe('Migrations', function () {
 
     it('Detects non-existent db', async function () {
diff --git a/src/backend/tests/scrobbler/scrobblers.test.ts b/src/backend/tests/scrobbler/scrobblers.test.ts
index 9c14de3..7f71f67 100644
--- a/src/backend/tests/scrobbler/scrobblers.test.ts
+++ b/src/backend/tests/scrobbler/scrobblers.test.ts
@@ -96,7 +96,7 @@ describe('Networking', function () {
                 }
             ));
 
-        it('Should set as unauthenticated with no possibility to retry if error is not network related',
+        it('Should record an authentication failure if error is not network related',
             withRequestInterception(
                 [
                     http.get('http://example.com', () => {
@@ -115,6 +115,53 @@ describe('Networking', function () {
                     assert.isTrue(authScrobbler.authFailure);
                 }
             ));
+
+        it('Should keep interactive clients gated until authentication is forced', async function() {
+            const authScrobbler = new TestAuthScrobbler();
+            authScrobbler.requiresAuthInteraction = true;
+            let attempts = 0;
+            authScrobbler.doAuthentication = async () => {
+                attempts++;
+                throw new Error('Invalid interactive credentials');
+            };
+
+            await expect(authScrobbler.testAuth()).to.be.rejected;
+            expect(attempts).eq(1);
+            await expect(authScrobbler.testAuth()).to.be.rejectedWith('user interaction is required');
+            expect(attempts).eq(1);
+
+            authScrobbler.doAuthentication = async () => {
+                attempts++;
+                return true;
+            };
+            await authScrobbler.testAuth(true);
+
+            expect(attempts).eq(2);
+            assert.isFalse(authScrobbler.authGated());
+        });
+
+        it('Should retry authentication failures for unattended clients',
+            withRequestInterception(
+                [
+                    http.get('http://example.com', () =>
+                        HttpResponse.json({error: 'Invalid API Key'}, {status: 401}))
+                ],
+                async function() {
+                    const authScrobbler = new TestAuthScrobbler();
+                    try {
+                        await authScrobbler.testAuth();
+                    } catch (e) {
+
+                    }
+                    assert.isTrue(authScrobbler.authFailure);
+
+                    authScrobbler.doAuthentication = async () => true;
+                    await authScrobbler.testAuth();
+
+                    assert.isFalse(authScrobbler.authGated());
+                    assert.isFalse(authScrobbler.authFailure);
+                }
+            ));
     });
 });
 
diff --git a/src/backend/utils/ListenFetchUtils.ts b/src/backend/utils/ListenFetchUtils.ts
index 1ee078a..8577274 100644
--- a/src/backend/utils/ListenFetchUtils.ts
+++ b/src/backend/utils/ListenFetchUtils.ts
@@ -33,11 +33,11 @@ export const createGetScrobblesForTimeRangeFunc = <T extends PaginatedTimeRangeS
                 const reqOptsHint: string[] = [
                 ];
                 if(currOpts.to !== undefined && currOpts.from !== undefined) {
-                    reqOptsHint.push(`Between ${todayAwareFormat(dayjs(currOpts.from))} and ${todayAwareFormat(dayjs(currOpts.to))}`);
+                    reqOptsHint.push(`Between ${todayAwareFormat(dayjs.unix(currOpts.from))} and ${todayAwareFormat(dayjs.unix(currOpts.to))}`);
                 } else if(currOpts.to) {
-                    reqOptsHint.push(`Until ${todayAwareFormat(dayjs(currOpts.to))}`);
-                } else if(currOpts.to) {
-                    reqOptsHint.push(`From ${todayAwareFormat(dayjs(currOpts.from))}`);
+                    reqOptsHint.push(`Until ${todayAwareFormat(dayjs.unix(currOpts.to))}`);
+                } else if(currOpts.from) {
+                    reqOptsHint.push(`From ${todayAwareFormat(dayjs.unix(currOpts.from))}`);
                 }
 
                 if(currOpts.limit !== undefined) {
